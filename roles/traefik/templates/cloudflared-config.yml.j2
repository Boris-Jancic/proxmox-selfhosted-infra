tunnel: {{ cloudflared_tunnel_credentials.TunnelID }}
credentials-file: /etc/cloudflared/{{ cloudflared_tunnel_credentials.TunnelID }}.json

ingress:
  # All tunnel traffic → Traefik; Traefik routes by Host header to backends.
  - service: http://localhost:80
